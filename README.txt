Hidden Markov model: Mobile robot in an inaccessible place

Execution:
$>python main.py
    Options:    -o <path to output directory>   Change default (./Output/) output path
                -v                              Verbose mode

Requirements:
    Python 3.5.2
    Libraries:
        image==1.5.5
        matplotlib==2.0.0
        numpy==1.12.0
